<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.UpaxPruebaTecnica" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/red</item>
        <item name="colorSecondary">@color/white</item>
        <item name="colorAccent">@color/black</item>
        <item name="android:navigationBarColor">@color/red</item>
        <item name="colorContainer">@color/red</item>
        <item name="android:titleTextColor">@color/white</item>
        <item name="backgroundTint">@color/red</item>
        <item name="android:statusBarColor">@color/red</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
        <item name="actionOverflowButtonStyle">@style/DotsDarkTheme</item>

    </style>

    <style name="TitleAppearance" parent="TextAppearance.Material3.ActionBar.Title">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">25sp</item>
    </style>

    <style name="DotsDarkTheme" parent="@style/Widget.AppCompat.ActionButton.Overflow" >
        <item name="android:tint">@color/white</item>
    </style>

    <style name="Theme.UpaxPruebaTecnica" parent="Base.Theme.UpaxPruebaTecnica" />
</resources>